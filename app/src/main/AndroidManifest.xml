<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alvarengadev.alvamessenger">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.activitys.ChatActivity"/>
        <activity android:name=".view.activitys.ProfileActivity" />
        <activity android:name=".view.activitys.FriendsActivity" />
        <activity android:name=".view.activitys.LoginSuccessfullyActivity" />
        <activity android:name=".view.activitys.SignUpActivity" />
        <activity android:name=".view.activitys.LoginActivity" />
        <activity android:name=".view.introscreens.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activitys.SignInActivity" />
        <activity android:name=".view.activitys.HomeActivity" />
    </application>

</manifest>