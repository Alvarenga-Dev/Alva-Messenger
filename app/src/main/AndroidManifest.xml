<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alvarengadev.alvamessenger">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.activities.MessagesActivity" />
        <activity android:name=".view.activities.ProfileActivity" />
        <activity android:name=".view.activities.FriendsActivity" />
        <activity android:name=".view.activities.LoginSuccessfullyActivity" />
        <activity android:name=".view.activities.SignUpActivity" />
        <activity android:name=".view.activities.LoginActivity" />
        <activity android:name=".view.introscreens.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activities.SignInActivity" />
        <activity android:name=".view.activities.HomeActivity" />
    </application>

</manifest>